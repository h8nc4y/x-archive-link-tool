---
name: bounded-implementation
description: 承認済み、または安全に限定された1 checkpointを最小差分で実装する。対象ファイルが明確で、既存挙動を保ったまま小さく変更するときに使う。
---

# Bounded Implementation

- 1 checkpoint の範囲に限定して実装する。
- 対象ファイルを絞り、指定外のファイルを変更しない。
- 仕様外改善、広範なリファクタ、依存追加、フォーマットのみの変更を避ける。
- 既存挙動、公開API、ドキュメント上の約束を壊さない。
- 不明な仕様は補完せず「未確認」と書く。
