---
name: commit-packaging
description: commit可能な変更一式を整理し、commit前確認材料を作る。変更要約、検証結果、Suggested commit messageを出すが、git commitやpushはしないときに使う。
---

# Commit Packaging

- commit 可能な変更一式を整理する。
- `git add .` は禁止する。
- `git commit` と `git push` はしない。
- 変更要約、検証結果、未確認事項、残リスクをまとめる。
- Suggested commit message を提示する。
